const server = require('express')()

module.exports = class Runner {
    constructor(){
        this.events = []
    }

    startOn(port){
        console.log(EventMap);
        server.listen(port, () => {

        })
    }

    dispatchEvent(event){

    }

    start(){
        
    }

    runEvents(events){
        events.forEach(event => {
            
        });
    }


}
