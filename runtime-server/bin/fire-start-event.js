#!/usr/bin/env node

const {default: goTime} = require('../src/index')

goTime(process.argv);

// import esm from 'esm'
// esm((module /*, options*/))
// import goTime from '../src/index'
// goTime(process.argv)

// import {goTime} from '../src'
